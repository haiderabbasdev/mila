services:
  - type: web
    name: mila
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.15.0
      - key: TENOR_API_KEY
        sync: false
    plan: free
    healthCheckPath: /
    autoDeploy: true
